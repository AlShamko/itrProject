enum Role {
  USER
  ADMIN
}

model User {
  id          String      @id
  email       String      @unique
  role        Role        @default(USER)
  isBlocked   Boolean     @default(false)
  inventories Inventory[]
}